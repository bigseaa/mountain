1.此module的功能为模拟了一个登录；
2.涉及到的有五张表，用户表、角色表、用户角色关联表、权限表、角色权限关联表；
3.流程：当使用账号密码调用登录（/user/login）接口后，会根据传过来的参数去查询用户表；若登录成功将该用户的
信息通过jwt提供的算法转为字符串形式，以此作为token返给调用者；接着调用者将token放入header中再次调用查询
权限的方法（/user/profile），通过此方法查询该用户所关联的所有角色，通过遍历所有角色查询出该用户的所有权限，
最后将包装对象返给调用者。
4.登录、鉴权的入口在UserController中。